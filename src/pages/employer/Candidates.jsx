import React from 'react';
import { Star, MapPin, MessageSquare, Search, Filter } from 'lucide-react';

const Candidates = ({ candidates }) => (
  <div className="space-y-6">
    {/* Candidats */}<h1>condidat s</h1>
  </div>
);

export default Candidates;