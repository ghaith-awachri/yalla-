import React from 'react';
import { Filter, Download, Star, MessageSquare } from 'lucide-react';

const Applications = ({ recentApplications, handleApplicationAction, getApplicationStatusColor, getApplicationStatusText }) => (
  <div className="space-y-6">
    {/* Candidatures */}<h1>aplications</h1>
  </div>
);

export default Applications;